[project]
name = "junios-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.138.0",
    "pydantic>=2.13.4",
]

[tool.fastapi]
entrypoint = "main:app"
